{
  "name": "noderender",
  "version": "1.0.0",
  "description": "node render",
  "main": "index.js",
  "scripts": {
    "test": "node --env-file .env index",
    "dev": "nodemon -L --env-file .env index",
    "start": "node --env-file .env index"
  },
  "author": "dtorres1000",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
